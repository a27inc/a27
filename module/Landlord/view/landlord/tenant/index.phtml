<?php
	$title = 'Tenant Table';
	$this->headTitle($title);
?>

<div class="row">
	<h1 class="pull-left"><?php echo $this->escapeHtml($title); ?></h1>
    <a href="<?php echo $this->url('tenant/add'); ?>" 
        type="button" class="btn btn-primary pull-right">
            Add Tenant</a>
</div>

<table id="tenant-table" class="table table-striped table-bordered">
	<thead>
		<th>First</th>
		<th>Middle</th>
		<th>Last</th>
        <th>Code</th>
        <th>Action</th>
	</thead>

<?php foreach($tenants as $t){
	echo '<tr>';
	 	echo '<td>'.$this->escapeHtml($t->first_name).'</td>';
	 	echo '<td>'.$this->escapeHtml($t->middle_initial).'</td>';
        echo '<td>'.$this->escapeHtml($t->last_name).'</td>';
        echo '<td>'.$this->escapeHtml($t->code).'</td>';
	 	echo '<td>
		 		<a href="'.$this->url('tenant/edit', array('action' => 'edit', 'id' => $t->id)).'">'.
		 			$this->escapeHtml('Edit').'</a>
	 			&nbsp;&nbsp;&nbsp;
	 			<a href="'.$this->url('tenant/delete', array('action' => 'delete', 'id' => $t->id)).'">'.
		 			$this->escapeHtml('Delete').'</a>
			</td>';
 	echo '</tr>'; 
} ?>
</table>

<script type="text/javascript">
	$(document).ready(function(){
		$('#tenant-table').dataTable();
	});
</script>